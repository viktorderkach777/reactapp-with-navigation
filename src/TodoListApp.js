import React, { Component } from 'react';
import './App.css';

export default class TodoList extends Component {
  render() {
    return (
      <div>
          <h2>TodoList component works!!</h2>
      </div>
    );
  }
}